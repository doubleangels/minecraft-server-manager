<div class="grid min-h-[50vh] place-items-center">
  <div class="text-center">
    {{! semantic token - raw 400/500 palette steps fail contrast on the light canvas }}
    <div class="pixel text-4xl text-danger">{{code}}</div>
    <h2 class="mt-4 text-lg font-semibold">{{title}}</h2>
    <p class="mt-1 text-sm text-ink-faint">{{message}}</p>
    <a href="/" class="btn btn-primary mt-5">{{{icon 'arrow-left' 'size-4'}}} Back to the Dashboard</a>
  </div>
</div>
