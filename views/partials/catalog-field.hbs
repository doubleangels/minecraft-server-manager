{{!-- One field-catalog entry → labeled control. Context: a catalog field object.
    NB: every default/min/max test goes through isDefined - Handlebars {{#if}}
    treats 0 as falsy, which silently dropped min="0" constraints and showed
    "image default" placeholders for real zero defaults. --}}
{{#if (eq type 'boolean')}}
  {{! Booleans: single row - text block left, toggle right (like Settings).
      The whole row is the <label>, so clicking the text flips the toggle. }}
  <label class="flex cursor-pointer items-start justify-between gap-6 py-3.5 first:pt-1 last:pb-1">
    <div class="min-w-0">
      <span class="flex flex-wrap items-baseline gap-x-2 gap-y-1 text-sm font-medium text-ink">
        {{label}}
        <span class="font-mono text-[11px] font-normal text-ink-faint">{{key}}</span>
        {{#if note}}<span class="badge badge-note badge-warn">{{note}}</span>{{/if}}
        {{#if danger}}<span class="badge badge-note badge-danger">Caution</span>{{/if}}
      </span>
      {{#if help}}<p class="mt-1 max-w-prose text-xs leading-relaxed text-ink-faint">{{help}}</p>{{/if}}
    </div>
    <span class="msm-toggle mt-0.5 shrink-0">
      <input type="checkbox" data-catalog-key="{{key}}" data-catalog-scope="{{scope}}" data-catalog-type="boolean" data-catalog-default="{{this.default}}" data-label="{{label}}" {{#if conflictsWith}}data-catalog-conflicts="{{conflictsWith}}"{{/if}} {{#if default}}checked{{/if}}>
      <span></span>
    </span>
  </label>
{{else}}
  <div class="space-y-1.5 py-3.5 first:pt-1 last:pb-1">
    <label class="flex flex-wrap items-baseline gap-x-2 gap-y-1 text-sm font-medium text-ink">
      {{label}}
      <span class="font-mono text-[11px] font-normal text-ink-faint">{{key}}</span>
      {{#if unit}}<span class="text-xs font-normal text-ink-faint">({{unit}})</span>{{/if}}
      {{#if note}}<span class="badge badge-note badge-warn">{{note}}</span>{{/if}}
      {{#if danger}}<span class="badge badge-note badge-danger">Caution</span>{{/if}}
    </label>

    {{#if (eq type 'enum')}}
      <select class="input max-w-sm" data-catalog-key="{{key}}" data-catalog-scope="{{scope}}" data-catalog-type="enum" data-catalog-default="{{this.default}}" data-label="{{label}}">
        {{#unless (isDefined default)}}<option value="">Use the default</option>{{/unless}}
        {{#each options}}
          <option value="{{value}}" {{#if (eq value ../default)}}selected{{/if}} {{#if desc}}data-desc="{{desc}}"{{/if}}>{{label}}</option>
        {{/each}}
      </select>
    {{else if (or (eq type 'number') (or (eq type 'size-mb') (eq type 'range')))}}
      <input class="input max-w-48 font-mono" type="number" inputmode="numeric"
        data-catalog-key="{{key}}" data-catalog-scope="{{scope}}" data-catalog-type="number" data-catalog-default="{{this.default}}"
        {{#if (isDefined min)}}min="{{min}}"{{/if}} {{#if (isDefined max)}}max="{{max}}"{{/if}} {{#if (isDefined step)}}step="{{step}}"{{/if}}
        placeholder="{{#if (isDefined default)}}{{this.default}}{{else}}Default{{/if}}">
    {{else if (eq type 'list')}}
      <textarea class="input max-w-xl font-mono text-xs" rows="2"
        data-catalog-key="{{key}}" data-catalog-scope="{{scope}}" data-catalog-type="list"
        placeholder="{{#if (isDefined default)}}{{this.default}}{{else}}One entry per line{{/if}}"></textarea>
    {{else}}
      <input class="input max-w-xl font-mono" type="text"
        data-catalog-key="{{key}}" data-catalog-scope="{{scope}}" data-catalog-type="text" data-catalog-default="{{this.default}}"
        placeholder="{{#if (isDefined default)}}{{this.default}}{{else}}Default{{/if}}">
    {{/if}}

    {{#if help}}<p class="max-w-prose text-xs leading-relaxed text-ink-faint">{{help}}</p>{{/if}}
  </div>
{{/if}}
